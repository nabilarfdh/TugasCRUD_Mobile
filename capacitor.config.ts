import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TugasCRUD_Mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
